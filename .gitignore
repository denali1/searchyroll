# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local only — never pushed
ROADMAP.md
AGENTS.md

# Build artifacts
*.zip
*.xpi
*.crx
web-ext-artifacts/
dist/

# Vercel (proxy deploy — local project link and auth cache)
.vercel/

# Environment
.env
.env.local

# Node (extension is vanilla JS — no runtime deps)
package.json
package-lock.json
node_modules/

# AI toolchain (local sessions)
skills-lock.json
.agents/
.claude/
.opencode/
